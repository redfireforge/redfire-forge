# gRPC Form Redesign Mockups

These are standalone design research mockups only (no app integration):

- `strategy-a-guided-cards.html` — progressive, card-based guided form
- `strategy-b-dual-pane-schema.html` — schema tree + focused editor panel
- `strategy-c-json-first-assist.html` — JSON-first with visual branch assistants
- `index.html` — launcher and comparison notes

Open in Chrome:

```bash
open -a "Google Chrome" docs/mockups/grpc-form-redesign/index.html
```
