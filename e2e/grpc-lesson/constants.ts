/** GRPC demo lesson metadata for E2E (keep in sync with grpc-lesson-contract/roster.ts). */
export const GRPC1_LESSON = {
  id: 'grpc-first-call',
  name: 'Your First gRPC Call',
  steps: 8,
} as const;
