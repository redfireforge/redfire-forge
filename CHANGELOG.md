# Changelog (Active)

This is the lightweight active changelog.

For full historical entries, see:
- CHANGELOG.archive.md

Format follows Keep a Changelog and Semantic Versioning.

---

## [Unreleased]

### Fixed
- **Pattern Toolbox — Applied condition always said `pathParam` / `id`** — the wand on a Cookie `sid` regex still previewed a path parameter, and Numeric ID stayed highlighted after typing `^S-[0-9]{4}$`, so `42` “Should match” looked like the pattern was wrong. Applied condition now names the open row; the library highlight follows the live expression; the session-cookie step rewrites samples before the Safety hold.
- **AM-05 prove-query Acting timed out at 45s** — after Run, Simulate parks on Results; Save as sample switches back to Request and unmounts the verdict. Acting then waited 20s twice for `SIMULATE_RESULT`. The shared helper returns to Results after Run, Ad-hoc form waits are capped, and AM-05 skips Save (the step teaches MATCHED / UNMATCHED, not samples).
- **AM-08 reject-multiple timed out at 45s** — Settings plus a full Simulate review, Save as sample, and a retry when the verdict was not AMBIGUOUS exceeded the Acting cap. The step no longer saves a sample, drops duplicate holds, and retries without the review tour.
- **Simulate — reject-multiple still answered 200** — a leftover saved `GET /catalog` (or mixed-case `X-Api-Version` on that sample) ran instead of the overlapping Ad-hoc request, so only the catch-all matched. Saved-sample headers are lower-cased before matching; the loud-policy lesson remounts Simulate on Ad-hoc, applies Reject all multiple matches, and expects **AMBIGUOUS** / **409**.
- **Simulate — `X-Api-Version` never matched `x-api-version`** — Ad-hoc Run overwrote normalized (lower-cased) headers with the typed casing, so Regional at priority 20 looked like a near-miss and the catch-all at 10 won. Header keys are lower-cased for matching; the lesson also runs Ad-hoc before Save as sample.
- **API Mock export confirm — Runtime journal header bled through** — sticky TIME / METHOD / PATH sat above the Workspace JSON dialog. Studio modal overlays now stack above the journal, and the table header stays inside its scroller.
- **AM-22 Quick Test `Server definition with id is required`** — Start Mock Server rebuilt the companion POST through `httpFetch`, which dropped the JSON body (companion then saw `{}`). Web control calls now use native `fetch` like Studio, and start always copies `serverId` onto the definition before POST.
- **AM-22 Quick Test `ECONNREFUSED :5173`** — Start Mock Server prefixed control calls with the Vite origin, so `httpFetch` hit `:5173` instead of the companion. Control URLs stay relative `/api/mock` (proxied to `:3001`). The last step opens **Console** before Quick Test.
- **AM-22 canvas nodes stayed ringed** — connect, open-config, and step payoff each drew a spotlight (and left ReactFlow selection) on Start and every mock node. The lesson now rings palette, Fit View, and config fields only, then clears selection.
- **AM-22 mock-server pick was invisible** — a single Studio server auto-filled the field, so Start / Apply / Assert / Reset / Stop never opened the menu. New nodes stay on **Select server…**; each config clears a stale value, opens the list, and holds **Cart API**.
- **AM-22 canvas clipped the mock chain** — Fit View kept 100% zoom, so Reset and Stop sat off-screen. The lesson now fits with a lower zoom cap and extra padding so the full Start→Stop graph stays in frame beside the guide.
- **AM-22 / AM-24 workflow mock picker stayed “No Studio servers”** — gallery import remaps Cart API / Import sandbox ids. Start, Apply, Assert, Reset, and Stop now wait for the live Studio list and pick by name. The picker heals a stale or single-server id instead of looking empty.
- **Test Runner — API Mock Server stayed “No Studio servers”** — Test Runner stays mounted and only listened for gallery import. Studio save now publishes a persist event (and an in-memory snapshot); the fixture reloads that list when the runner tab is shown.
- **Test Runner — deleting every Studio mock left the old Server list** — an empty in-memory snapshot was ignored, so the fixture reread disk (still the pre-delete save). An empty publish is now treated as the live library.
- **API Mock Response body editor — vs-dark brown canvas** — Monaco’s default `#1e1e1e` no longer sits on the slate Studio chrome. The editor uses `--bg` / `--text` like Status, Name, and Content-Type.
- **Test Runner — API Mock fixture `useEffect` size warning** — hydrating the fixture grew the heal effect’s dependency list from 3 items to 8 (`enabled`, server id, isolate, `auto`, `stop`, …). Extra fields are read from a ref so the array stays length 3.
- **AM-23 fixture Run failed with `serverId is required`** — gallery import remaps Store’s id, and the lesson wrote the template id into the native select (which clears React state). The panel reloads on workspace change, heals a blank selection, and setup falls back to the active Studio server.
- **AM-21 fail-loudly showed PASS** — the scratch-pad `/health` run has no assertions, so its green PASS survived into the “wrong expectation must fail” step. The lesson now runs the saved health sample, and editing expected status updates the sidebar badge immediately.
- **API Mock Runtime Settings — clipped field hints** — Journal, Limits, and CORS help text was cut off by `overflow: hidden` cards in a stretching grid. Hinted rows now grow; short numeric hints sit beside the control.
- **AM-14 timeout prove showed the old 503** — the catch-fetch aborted through Vite `/__proxy`, which kept the mock socket open so the journal never wrote a fault row. Acting then clicked the earlier match-limit **503 retired**. `/__proxy` now aborts the upstream fetch when the browser disconnects; the timeout step waits for a **fault** row (status 0) and never selects a 503.
- **AM-13 state-live timed out on Studio** — Acting waited 45s for the Runtime **State** tab while the step had switched back to Studio, where that tab does not exist. The step now opens Runtime then the live cursor; Reset still starts on that dock before Simulate returns to Studio.
- **AM-12 prove-rules timed out; State step stayed on Studio** — two Simulate runs plus Rendered holds exceeded the 45s Acting budget, and the last step called Studio instead of Runtime so **State** never appeared. The second run skips Save as sample; the State step opens Runtime then the sequence cursor.
- **Console `/api/logs/stream` spam** — EventSource no longer native-retries empty Vite proxy drops (`net::ERR_EMPTY_RESPONSE`). The log stream closes on error and backs off, the `/api` proxy no longer times out idle SSE, and a down companion answers 204 so Chrome stops reconnecting.
- **Monaco `Canceled` console noise** — editor remounts during demo body patches reject with `Canceled`; those rejections are swallowed instead of showing as uncaught promises.
- **AM-11 variables step — Acting sat idle** — the step re-rang **Variables** for 1.4s after reading already spotlighted it, then waited up to 20s for a dock panel that React Strict Mode could reset to Transactions. Opening Variables now clicks immediately, falls back to the dock tab, skips waits when the inspector is missing, and Runtime keeps the requested tab across the remount.
- **AM-10 prove step — empty Runtime flash** — the last step waited a fixed 1.4s then opened Runtime, which is shorter than the 1.5s journal poll, so viewers briefly saw the empty “Waiting for the first request” page. The lesson now waits until the Live strip counts the GET (or a journal row exists) before switching views.
- **API Mock Studio — stop polling a missing listener** — when `/state` or `/transactions` returns 404 / `NOT_RUNNING` (lesson wipe, crashed companion, parked tab), Studio marks the server stopped instead of retrying every 1.5s. The console SSE also stays closed until a listener is actually running, so Chrome is not filled with 404 and `ERR_EMPTY_RESPONSE` noise.

### Changed
- **Condition body expand modal** — search stays in the header; a chrome bar shows JSON/Not JSON, line and char counts, Undo/Redo, and a Pretty / One line pair. The editor uses the Studio mono stack; the footer keeps ⌘F / Esc on the left and Cancel / Apply on the right.
- **Simulate Decision trace titles** — `/catalog` and candidate `GET /catalog` labels use Inter (`--am-ui`), not the leftover mono/serif stack.
- **Simulate Selection timeline badges** — outcome pills share one column width and sit vertically centered so `ok`, `Winner`, and response ids line up.
- **API Mock Outbound tab** — pipeline strip (Template → Transforms → Client, then Callbacks), section cards with sentence-case titles, dashed empty states, and callback cards that split Method / Timeout / Retries into two-tone rows. **+ Add** test ids are unchanged.
- **Test Runner — Mock Server host option** — the API Mock fixture is hidden until you pick **Mock Server** after Settings in the Host row. Original / Settings / Custom hide it again. There is no **Start with run** or **Override host → mock** checkbox; Mock Server starts the fixture and always sends the suite to the mock. **Isolate off** uses the Studio server and restores its prior Running/Stopped state after the suite.
- **Workflow — Start Mock Server config** — two-tone form rows (Listener / Outputs / On error), a real Isolate checkbox with On/Off hints, and no leaked HTTP Query Parameters / Initial Variables. The server list reloads when Studio saves or deletes mocks.
- **Workflow — Assert Mock Calls config** — grouped Target / Call count / Last matching call / Request headers cards. Exact/min/max stay on one compact row; status, body, and recency are full fields. Body is a resizable textarea with Contains / Equals / Regex. Request headers are a list (**+ Add** / Remove). Blank value means the name must be present. Existing single-header nodes still run.
- **Workflow — Reset Mock State config** — Listener (server + on error) is a two-tone form; Clears is a separate card of three fact tiles (scenario state, sequence cursors, match counters). No partial reset.
- **Test Runner — API Mock Server dropdown** — uses `CustomSelect` instead of a native `<select>`, matching Studio and the rest of the app. Each option shows live **Running** / **Stopped** from the companion (Studio start/stop updates the list).
- **Test Runner — API Mock fixture** — two-tone rows with the checkbox beside its hint, matching Host and Execution Mode. Global `input { width: 100% }` no longer stretches the toggles across the card.
- **AM-22 Quick Test failed on Start** — Start Mock Server often saved with no server (`pick server`), so isolate never published `mockBaseUrl` and HTTP hit the env host (404). The lesson now writes Cart API + isolate + port/base-URL vars after Save. Workflow mock nodes use the shared HTTP transport, and unresolved `{{templates}}` are not prefixed with the environment base URL.
- **AM-22 palette step searches Mock** — step 1 types **Mock** in Search all blocks, holds the field, then each matching block (Start, Apply, Reset, Stop, Assert). It no longer walks the Actions rail and API Mock chip.
- **API Mock Examples — Body exact** — the field is a resizable JSON editor (not a one-line input) with a **Pretty format** badge, matching callback / import paste.
- **AM-18 prove-example step** — Examples opens quietly during Preparing. The reading ring is on **Simulate**, not the Examples tab, so that tab no longer pulses while Match is still on screen.
- **AM-18 journal miss / create-route — no view flash** — when Runtime already has the journal, those step guards stay there. Create route no longer re-opens Runtime Settings (which flashed Settings then Transactions).
- **API Mock CLI handoff uses `redfireforge`** — export confirmation and the rules footer copy `redfireforge mock simulate` / `redfireforge mock verify`. There is no `cli` binary.
- **API Mock import lessons — Pretty format** — after every JSON paste (OpenAPI, WireMock, HAR, last export) the lesson clicks **Pretty format** and holds the readable payload before Parse.
- **API Mock Studio — resizable rules panel** — drag the vertical bar between the rules list and the editor (or use arrow keys when it is focused). Width persists across sessions. Hidden on the narrow-viewport drawer.
- **API Mock rules footer tally** — enabled vs draft is two status chips (live green / draft amber) instead of faint inline text. Lessons still read `N enabled · M draft(s)`.
- **API Mock gallery presets split** — `src/data/galleries/api-mock/presets.ts` is a barrel again. Factories live in focused modules (`presets-helpers`, getting-started, matching, conflicts, responses, simulation) so the product monolith gate stays under 900 lines.
- **API Mock dribble chunks — empty delays** — an empty chunk row is a pause with no bytes, not leftover JSON that flushes at End stream. The schedule hint and placeholder say so; Rendered still shows the intended full body, Trace shows the wire. The timing-faults lesson’s last step now spells out **FAULT: dribble**, empty gaps, and why the sample is FAIL.
- **API Mock Expires at — calendar picker** — Timing’s expiry field keeps typed ISO and the +1h / +24h / +7d chips. The calendar control opens a Studio-themed month grid and 24-hour time row (Clear / Today / Apply) instead of the native browser datetime picker.
- **API Mock response cookies — flag meanings** — Headers & cookies lists what **HttpOnly**, **Secure**, and **SameSite** Strict / Lax / None do, and the SameSite menu repeats a one-line hint per option.
- **API Mock Pattern Toolbox — XPath tab layout** — presets stay on the left; **Sample XML** fills the remaining width, and **Generated matcher** sits underneath so XPath / Resolved / Equals are full-width fields instead of a 268px side column that truncated the values. The ✓ / × verdict is a badge beside Equals value.
- **API Mock — expand Body to a full-text popup** — Simulate's request **Body**, Match **Body** expected/schema fields, and Pattern Toolbox **Sample XML** gain an expand control. The popup shows the full payload with search, pretty-print JSON, undo/redo, and Apply, so multipart bodies, SOAP envelopes, and JSON matchers are readable without stretching the compact row.
- **API Mock Simulate — From rules probes** — sidebar **From rules** entries are suggested probes from mock rules, not saved samples. Selecting one now shows the request (read-only) instead of an empty pane, with **Edit in Ad-hoc** to copy it into the scratch pad.
- **API Mock Simulate — Save as sample names after save** — **Save as sample** stores the full request (method, path, headers, body) under **Saved samples**, then focuses the name field so you can rename it. Reopening a saved sample restores that request. Demo lessons AM-04…AM-08 now save and name the probe before **Run simulation**.
- **AM-07 payload-formats lesson pacing** — longer spotlight holds, an aim ring before every tab/modal, and a review pass over path / Content-Type / body plus a ~2s hold on **Run simulation** before the click so the viewer can read the request that is about to be judged.
- **AM-06 body-matching lesson pacing** — spotlight holds are longer, every tab switch / toolbox / Simulate open rings the trigger for ~1.4s *before* the click, and Simulate now reviews the filled path and body then holds on **Run simulation** before running.
- **AM-05 request-predicates lesson pacing** — condition authoring, toolbox samples, and especially Simulate's decision-trace now hold longer (each trace leaf ~1.4s, MATCHED/UNMATCHED ~1.8s) so a first-time viewer can follow which predicate ticked. Simulate also reviews the filled path (and headers, when present) then holds on **Run simulation** before the click.
- **API Mock response editor — status reason, body kind, and binary hint** — the reason phrase next to the status code is now editable (quick chips still fill the default). Changing **Content-Type** also sets the body kind (JSON / HTML / XML / text / base64). `application/octet-stream` shows a notice that the body is base64 decoded to bytes on the wire. Header and cookie rows gained stable test ids for the response-content lesson.
- **AM-24 capstone lesson pacing** — OpenAPI import as drafts, JSONPath Simulate proof, faker + Format preview, 404 variant then sequence/rules, delay + timeout fault, Analyze + priority, Run all green, live journal near-miss, workspace/WireMock export, and Workflow Quick Test use the same slower holds as AM-04…AM-23. The reading highlight is not re-rung before the first click; reveal waits cap at 8s. Companion required; no Docker.
- **AM-23 harness-ci lesson pacing** — Test Runner fixture panel, Store server, Isolate on (throwaway copy; Off restores Studio's prior Running/Stopped), isolated run start line + results, Stopped / freed port, Runtime journal, workspace export confirmation, and `cli mock simulate` / `cli mock verify` use the same slower holds as AM-04…AM-22. The reading highlight is not re-rung before the first click; reveal waits cap at 8s. Companion required; no Docker.
- **AM-21 simulation-suite lesson pacing** — scratch-pad Simulate, three result views, an editable expected status, FAIL, run-all with sequential state, a replay seed run twice, export-trace confirmation, then Attach + Try in Requests use the same slower holds as AM-04…AM-20. The reading highlight is not re-rung before the first click; reveal waits cap at 8s. Offline; no companion Start, no Docker.
- **AM-20 tls-mtls lesson pacing** — Generate self-signed (2000ms PEM hold), inspect cert + private-key note, Save + Start with `https://` and HTTP/2, live GET `/health` 200, mTLS client bundle (2000ms issued hold), security `certSubject` condition, Simulate match then miss, export redaction + Stop use the same slower holds as AM-04…AM-19. The reading highlight is not re-rung before the first click; reveal waits cap at 8s. Companion required; no Docker.
- **AM-19 runtime-ops lesson pacing** — CORS preflight, limits, redaction + proof, persist + diagnostics p95, console lines, outbound setHeader + callback allowlist, and the injected `X-Mocked-By` header use the same slower holds as AM-04…AM-18. The reading highlight is not re-rung before the first click; reveal waits cap at 8s. Companion required; no Docker.
- **AM-17 proxy-record lesson pacing** — Settings → Proxy, allowlist, private-net fence, unmatched Proxy, Start, a proxied journal row, the recorded draft, and closest-match use the same slower holds as AM-04…AM-16. The reading highlight is not re-rung before the first click; reveal waits cap at 8s. Companion + Docker echo (`docker/api-mock`, :4017) required.
- **AM-16 export lesson pacing** — six export shapes, redaction proof, WireMock loss, HAR count, native round-trip as copy, and the footer CLI line use the same slower holds as AM-04…AM-15. The reading highlight is not re-rung before Export / Close; confirm waits cap at 3s so Acting cannot hit the 45s DemoHub timeout. Export menu clicks are immediate (no ripple gap that closes the dropdown). Live demos skip the OS file download so Chrome's multi-file prompt cannot freeze Acting. The listener never starts.
- **API Mock journal poll after a demo wipe** — an in-flight hydration `list()` can no longer re-mark wiped servers as running and spam `/transactions` 404s. Journal/state/draft fetches now time out at 2.5s.
- **API Mock Export menu stays open during demos** — mousedown on the live-demo panel, spotlight ring, or click ripple no longer dismisses the Export dropdown.
- **AM-15 WireMock sample is lossless** — the pasted stub uses `equalTo` on `X-Tenant` and `page` plus `fixedDelayMilliseconds` only. The step holds the mapped preview (`GET /orders/99`); a loss report no longer appears for a sample that Studio can keep.
- **AM-15 import lesson pacing** — seven sources, generalize, drafts, OpenAPI, WireMock preview, HAR, Catalog/Requests, the Replace warning, and the enable-and-prove fetch use the same slower holds as AM-04…AM-14. Tab/dock/modal/import-panel triggers ring before the click; the listener stays down until the final Apply + matched journal row.
- **AM-14 timing-faults lesson pacing** — delay/jitter, Simulate virtual delay, live duration, match-limit fall-through, +1h expiry, the five fault cards, a caught timeout, reset, and the dribble timeline use the same slower holds as AM-04…AM-13. Tab/dock/modal triggers ring before the click; Apply and the journaled fetches (including the aborted timeout) are the live payoff.
- **AM-13 stateful lesson pacing** — state fields, the counter row, live State-tab chips, Reset, Run all samples, Weighted 90/10, and the masked variable use the same slower holds as AM-04…AM-12. Tab/dock/modal triggers ring before the click; Apply and the two journaled fetches are the live payoff.
- **AM-12 variants-sequence lesson pacing** — variant cards, the 404 chip, JSONPath conditions, Simulate 404-then-200 (each run opens **Rendered response** and holds the body), Sequence, three journaled fetches, and the Runtime **State** cursor use the same slower holds as AM-04…AM-11. Tab/dock/modal triggers ring before the click; Apply and the wrap-around journal rows are the live payoff.
- **AM-11 templating lesson pacing** — Monaco `{{` completions hold 2000ms, faker preview 1500ms, the broken-helper diagnostic holds 3200ms so the error is readable before it is replaced, and every other beat uses the same slower holds as AM-04…AM-10. Tab/dock/modal triggers ring before the click; Apply and the two journaled responses are the live payoff.
- **API Mock response preview evaluates templates** — when the body contains `{{helpers}}`, the right-hand preview renders them against a sample request derived from the rule path (so `GET /products/:id` previews as `/products/42`). Unknown helpers show an inline diagnostic instead of an empty field.
- **AM-09 conflict-inspector lesson pacing** — Analyze, kind filters, dimension rows, and Simulate AMBIGUOUS use the same slower holds as AM-04…AM-08. Filter/view switches ring the trigger before the click; the witness request is reviewed and **Run simulation** is held before it runs; Simulate closes before the next inspector beat.
- **AM-08 selection-policy lesson pacing** — nested groups, Selection settings, and Simulate now use the same slower holds as AM-04…AM-07. After filling Simulate (and after choosing a policy), the lesson pauses on the values and then on **Run simulation** before clicking it.
- **AM-04 Path Matching lesson pacing** — spotlight holds on kind badges, toolbox verdicts, and Simulate MATCHED/UNMATCHED are roughly doubled so a first-time viewer can read each ring before the next beat. After Simulate fills the path, the lesson now holds on the filled value and then on **Run simulation** before clicking it.
- **API Mock chrome** — dropped the redundant “API Mock Studio” title/tagline from the tab strip; the protocol tab already names the view, matching GraphQL/gRPC/Kafka.
- **API Mock settings — refuse a port another mock already owns** — typing a listen port claimed by another saved server (open tab or parked) shows an inline error naming the owner and disables **Save settings**, instead of applying the collision and failing later with ERROR on Start.
- **AM-03 closing step** — **Analyze all** now opens the Conflict Inspector, so the health-check step follows that page instead of waiting ~16s on the unmounted explorer badge and Conflicts-only filter.
- **API Mock Conflict Inspector** — competing-rule cards, Match dimensions, and Selection policy share one two-column grid (no **vs** gutter). **Open in Studio** sits on the route row as a filled control; dimension rows stay compact with leftover space below; policy values use the UI sans font.
- **API Mock — body conditions state what they read** — the key box on a `body` source condition is now disabled and shows `(whole body)` with an explanatory tooltip. Body matchers always read the entire payload (form and multipart field names belong in the matcher's own field box), and an editable key box implied a level of addressing that was silently ignored. The XPath tab of the Pattern Toolbox also gained a live **Resolved** read of the expression against the sample document and a ✓ / × verdict, matching the JSON body tab — previously a wrong expression only surfaced later as a rule that never fired.
- **API Mock — selection policy is now inspectable** — `selectRoute` fills `policyDecision.specificityBreakdown` when two or more rules tie at the highest priority (the field existed on the contract but was never populated). Simulate shows a **Winner** badge and the score list; the Selection settings tab gained an editable **Ambiguous response** body (`{{requestId}}` / `{{competingRuleCount}}`); None-of groups show a fail-closed note so a stub can never invert into a match.
- **Demo test harness review** — revised WS/Kafka lesson helpers, harness workflow runner, `th-workflow-runner.ts`, WebSocket mock server, WebSocket connect panel and tab content, `useWebSocketMockServer`, `demoRipple`, `useDemoProgress`, `domVisibility`, `CustomSelect`, Kafka selectors, WebSocket selectors, and `websocket-studio.css`; aligned coverage-gap tests and E2E specs; 87 files updated across `packages/demo-hub`, `src/features/websocket`, `src/features/kafka`, `src-server/kafka`, and `src/shared/components`.
- **WS demo lesson pacing & correctness** — replaced instant field fills with `spotlightAndPause` rings across ws-stomp, ws-mock-server-advanced, ws-reliability, and ws-workspace lessons; added bullet-list step descriptions in ws-stomp and ws-graphql; fixed ws-reliability step 4 (auto-reconnect toggle is locked while connected — disconnect first); fixed ws-workspace env-resolve step navigation back to WS Studio after Env Manager; fixed ws-graphql to explain the inclusive 6-frame countdown for `start: 5`.
- **Coverage gates** — all 1816 product files now ≥ 90% on every metric: `CustomSelect.tsx` branches 87% → 90% (hidden-trigger close path + stable-position `setMenuPos` bail-out); `webhook-server.ts` funcs 89% → 93% (non-ok kafka-admin HTTP response + abort-timeout callback).

### Fixed
- **API Mock demo bridge — stale gallery catalog after HMR** — Demo Hub kept the catalog snapshot from the first `useEffect`, so new samples (`am-gallery-response`, `am-gallery-templating`) looked unknown until a full reload. Import now reads the live catalog on each call and re-binds `window.__demo*` after Fast Refresh. Wipe publishes an empty workspace *before* stopping listeners so the studio does not poll `/transactions` and `/state` on a server the companion already dropped (Chrome 404 spam).
- **Vite HMR — gallery catalogs under `src/data`** — `server.watch.ignored` used `**/data/**`, which also matched `src/data` and `src/features/*/data`. Gallery sample edits (including `am-gallery-response` for AM-10) never invalidated, so Demo Hub kept importing a stale catalog. Ignore is now only the repo-root runtime `data/` directory.
- **API Mock Conflict Inspector — fingerprint table** — Rule fingerprints is a two-column table (rule · SHA-256) with one row per competing rule, so identical paths still line up Left/Right next to their hashes.
- **API Mock Conflict Inspector — Acknowledge / Adjust priority footer** — the two actions sat in the scrolling finding (`position: sticky`), so they floated over Witness / fingerprints while you scrolled. They now live in a pinned bar at the bottom of the pane; the finding scrolls above them.
- **API Mock Conflict Inspector — Adjust priority menu** — Raise-left / Raise-right opened below the button at the bottom of a scrolling detail pane and were clipped out of view. The menu now opens upward above a sticky action bar and scrolls into view.
- **API Mock Conflict Inspector — Rule fingerprints** — the demo (and a programmatic `.click()`) targeted the `<details>` element, which does not toggle in the browser. The lesson now clicks **Rule fingerprints** itself and holds on the hashes.
- **API Mock Conflict Inspector — fingerprint hashes** — the inspector showed an 8-character prefix with an ellipsis. It now shows the full SHA-256 for each rule, plus a Same/Different badge. Duplicate means Match agrees; the hashes still differ when names, ids, or bodies differ.

### Added
- **API Mock settings — Proxy safety** — the Proxy tab now has a **Block private nets** toggle (`blockPrivateNetworks` is persisted on Save), a default-deny note while proxy is on, and a 508 loop-guard note. Docker echo fixture: `docker/api-mock` on `:4017`.
- **API Mock export confirmation** — every Export menu download opens a readable confirmation (pretty preview with search, redaction callout, TLS key / sensitive-variable placeholders, WireMock loss notes, HAR entry count, and a copyable `cli mock simulate` line). Native JSON/YAML keep a **Use last export** shortcut on Import, the Export menu is grouped (Workspace / This server / Interop), and the routes footer carries the same CLI command.
- **API Mock — Saved servers library (closing a tab no longer discards work)** — mock server definitions now live in a durable library that is independent of the tab bar. Closing a tab stops the listener and **parks** the server with its rules, examples, variables, and settings intact; the workspace envelope gains `openTabIds` (the open subset, in tab-bar order) alongside `servers` (the library), with `tabOrder` kept as a mirror so older builds and the CLI still load. A new title-bar **Saved servers** button (count + "N closed" hint) opens a searchable library dialog, and the studio shows a **library landing** list instead of the empty state when servers exist but no tab is open. Removing a definition is now an explicit **Delete Server…** (tab context menu or library row) behind a confirm, with a 5-second undo that restores the server *and* its tab. Parked servers keep their port claim until deleted. New modules: `apiMockServerLibrary.ts`, `useApiMockServerLibrary.tsx`, `ApiMockServerLibraryPanel/Modal.tsx`, `ApiMockLibraryLanding.tsx`; workspaces written before this change load unchanged (absent `openTabIds` means every saved server is open).
- **API Mock demo curriculum v2** — the v1 eight-lesson pack (`AM-1`…`AM-8`, one concept per step) is replaced by a 24-lesson scenario curriculum built one lesson at a time, with multi-beat steps (each step clusters several visible actions behind timed spotlights) and a live-authoring policy: Gallery presets seed a quiet background corpus, never the feature the lesson teaches. Shipped so far: **Studio Tour & Your First Mock** (`am-01-studio-tour`, 8 steps — create a server, author a rule, start, send real traffic, read the journal and transaction detail, stop) and **Multi-Server Workspace: Tabs, Ports & Binding** (`am-02-multi-server`, 8 steps — add a second server on an auto-picked port, F2-rename its tab, apply a base path and read the `0.0.0.0` LAN warning, bind both listeners, switch tabs, duplicate a server, reorder tabs against the 8-tab ceiling, and close a running server through Stop & Close), and **Rule Library: Folders, Search, Filters & Docs** (`am-03-rule-library`, 8 steps — read a twelve-rule tree, search by path and by tag, hide drafts and isolate a method, author a folder and drag a rule into it, disable without deleting, delete through the confirm and take it back inside the undo window, write `summary`/`operationId`/`tags` and prove the new tag is searchable, then Analyze the library and filter to the flagged overlap), and **Path Matching & the Pattern Toolbox** (`am-04-path-matching`, 7 steps — start from one recorded literal, rewrite it as `/products/:id` and watch the kind badge re-infer itself, prove in Simulate that the template matches a different id *and* a non-numeric one, tour the Pattern Toolbox as a workbench that changes nothing until Apply, generalize a recorded order path segment-by-segment and test it with an id you never recorded, contrast `/assets/*.png` with `/assets/**` before applying the glob, take the **Numeric ID** expression off the regex library shelf and anchor it to `^/products/[0-9]+$`, then prove the tightened matcher rejects `/products/abc` and still renders the body for `/products/42`), and **Query, Header, Cookie & Security Conditions** (`am-05-request-predicates`, 8 steps — start from a rule with an empty Match group, add a query condition and prove it both ways in Simulate (`query "page" exact failed — got "3"`), walk the operator vocabulary from `prefix` to `exact` on a tenant header, swap a hand-parsed `authorization` matcher for the **Security** source and its facet dropdown, turn a nested group into a **None of** guard holding a value-less `present` check, test a `^S-[0-9]{4}$` cookie pattern in the wand's toolbox until **Ignore case** flips a lower-case sample green, compose a header and a query constraint in the **Query & headers** tab and land both as rows in one **Add conditions** press, then prove all seven conditions in the decision trace — including why the `x-debug` row is red on a rule that matched), and **Body Matching: Subset, Strict, JSONPath & JSON Schema** (`am-06-body-matching`, 6 steps — read the corpus `json_subset` baseline and prove a payload with three unmentioned fields still matches, switch the same expected JSON to `json_strict` and watch the trace name the operator that rejected it before switching back, paste a payload into the toolbox's **JSON body** tab and *select* `"RF-100"` to derive `$.items[0].sku` with its resolved value, clear and refill **Expected** to show `jsonPath_exists` becoming `jsonPath_equals`, flip the row's **equals** button to **contains** so `RF-` covers a whole SKU family, land the **Required id** schema preset before pasting a real Ajv-validated contract, then prove that an order missing `customer.id` satisfies both JSON matchers and is rejected by the schema alone while the complete order renders its `201`), and **Forms, Multipart, XML & Binary Matching** (`am-07-payload-formats`, 7 steps — open four rules that answer *any* body and author a `form_field_exact` pair on a urlencoded token endpoint, watching the key box grey out to `(whole body)` because the payload itself is the value; prove it with the `application/x-www-form-urlencoded` header that makes the parse happen, then widen the same row to `form_field_regex` `^ada\.` so a regional `ada.lovelace.eu` matches without a second rule; match a multipart upload on a *text* part's value and a *file* part's **filename**, then run a real boundary-delimited body and read Normalized and the `201` in Rendered; let the toolbox's XPath tab compose `//*[local-name()='orderId']/text()` from a preset against the real namespaced SOAP envelope, with the live **Resolved** box proving the expression selects `A-1098` before Apply; add an `xmlSchema` element list instead of an XSD and watch a truncated envelope come back UNMATCHED with the `xpath_equals` row still ticking; and pin a firmware blob with `binary_exact` before re-pointing the same row at its SHA-256 digest, where one changed character flips the verdict), and **Boolean Groups, Priority & Selection Policy** (`am-08-selection-policy`, 8 steps — nest an Any-of tenant group under All of so the tree reads `version AND (eu OR us)`, add a None-of `x-debug` guard that fails closed, prove in Simulate that the same request still matches Default and comes back AMBIGUOUS, raise Regional to priority 20 so highest-priority picks a Winner, switch to reject-multiple so 20 does not save you and the 409 body is yours to shape, then restore equal priority and break the tie by specificity with a visible score breakdown), and **Conflict Inspector: Four Overlap Kinds** (`am-09-conflicts`, 8 steps — Analyze eight path-disjoint overlapping pairs into one Duplicate / Shadowed / Definite / Potential finding each, read fingerprints and the unknown regex dimension, simulate the GET /health witness to AMBIGUOUS, Open in Studio and return, raise Daily so Definite reclassifies to Shadowed, then acknowledge the remaining duplicate and bump a fingerprint so Re-analyze marks it Stale), and **Response Content: Status, Headers, Cookies & Body Kinds** (`am-10-response-content`, 8 steps — start from `GET /orders` answering plain `200 {}`, click 201 and type a custom reason phrase, Format a minified JSON body, add tracing and cache headers, name an HttpOnly cookie, read the rendered preview, tour HTML and base64 body kinds, then Apply the running listener and prove the real response in the journal), and **Dynamic Responses: Templates, Faker & Body Mapper** (`am-11-templating`, 9 steps — type `{{` for Monaco completions, echo path/query/header/cookie/jsonPath, mint uuid/now/randomInt/oneOf, grow a body with repeat, fill faker names, add a tenant server variable, Apply and fetch `/products/42` twice so the uuid changes, Map body with Auto-map, then surface and clear a broken helper), and **Response Variants: Rules & Sequence Modes** (`am-12-variants-sequence`, 8 steps — add a 404 sibling to `POST /cart`, set a JSONPath condition so a missing SKU wins that variant, mark the 200 as the one Default, prove both answers in Simulate, switch to Sequence, Apply, and fetch the same request three times so the journal wraps around while the State tab shows the live cursor), and **Stateful Mocks: State Machine, Counters & Weighted Chaos** (`am-13-stateful`, 8 steps — switch `POST /cart` to State, require EMPTY then HAS_ITEMS with an items counter, wire the second body, Apply and fetch so the journal and the State tab agree, Reset then Run all samples, switch to Weighted 90/10 with a replay seed so two runs match, and mark a tenant variable sensitive), and **Latency, Eligibility & Connection Faults** (`am-14-timing-faults`, 8 steps — delay/jitter, Simulate virtual delay, live duration, match-limit fall-through, +1h expiry, the five fault cards, a caught timeout, reset, and the dribble timeline), and **Import Everything: cURL, OpenAPI, WireMock, HAR, Catalog** (`am-15-import`, 9 steps — seven sources and three modes, generalize `/users/42` to `/users/:id`, drafts stay disabled, OpenAPI stubs, an honest WireMock loss report, HAR, Catalog/Requests promotion, the Replace warning, then Enable + Apply + a matched journal row), and **Export & Round-Trip: JSON/YAML, WireMock, HAR, Redaction** (`am-16-export`, 7 steps — six export shapes with a readable confirmation, redacted TLS key and sensitive variable, WireMock loss notes, HAR entry count, Import as copy duplicating rules, then the copyable `cli mock simulate` footer line), and **Proxy Passthrough & Record-to-Drafts** (`am-17-proxy-record`, 8 steps — Proxy tab enable + default-deny + echo allowlist, private-net fence then forward-auth, record drafts + unmatched Proxy, Start, a proxied journal row for `/widgets/42`, the recorded draft, enable + Apply + matched take-over, then the 508 loop-guard note and closest-match fallback), and **Journal Forensics: Near-Misses, Candidates & Promotion** (`am-18-journal`, 8 steps — store library running, two matched journal rows, filter empty then restore, a `/produts/42` unmatched near-miss, closest-match debug body, create-route seeded editor, save-example + Open in Requests, copy/export/clear, then Simulate the saved example), and **Runtime Ops: CORS, Limits, Redaction, Diagnostics & Console** (`am-19-runtime-ops`, 8 steps — store library running, CORS preflight 204 with an unchanged journal count, inbound/connection/drain caps, redact `authorization` and `$.password` then prove `[REDACTED]` on a live POST, persist + match p95, console start/commit lines, setHeader transform + callback allowlist, then Apply and hold `X-Mocked-By` on the response), and **HTTPS, HTTP/2 & mTLS with Cert-Subject Matching** (`am-20-tls-mtls`, 8 steps — plaintext health mock, Generate self-signed with a 2000ms PEM hold, inspect the private-key redaction note, Save + Start on `https://` with the HTTP/2 badge, live GET `/health` 200 over TLS, issue an `acme-client` mTLS bundle and Restart, pin security `certSubject`, Simulate match then UNMATCHED, export redacts the key, then Stop), and **Simulation as a Test Suite: Examples, Seeds, Assertions, Trace** (`am-21-simulation-suite`, 8 steps — eight saved samples, an ad-hoc scratch-pad run, decision trace / Normalized / Rendered, an edited expected status that FAILs, run-all with sequential state, a replay seed run twice, export-trace confirmation, then Attach + Try in Requests), and **Workflow Orchestration: Start → Apply → Reset → Assert → Stop** (`am-22-workflow`, 9 steps — empty Designer canvas, five mock palette blocks, Start with isolate + `mockPort`/`mockBaseUrl`, HTTP `{{mockBaseUrl}}/cart`, Apply, Reset, Assert journal min/status/body/recency, Stop, wire Start → Apply → HTTP → Assert → Reset → Stop, then Quick Test greens), and **Test Runner Fixtures & CI Handoff** (`am-23-harness-ci`, 7 steps — Test Runner fixture panel picks Store API, isolate + mock base URL, isolated run start line + results, Stopped / freed port, Runtime journal, workspace export confirmation, then copyable `cli mock simulate` / `cli mock verify`), and **Ship a Contract Mock** (`am-24-capstone`, 9 steps — OpenAPI drafts for Orders, JSONPath `$.sku`, faker 201, 404 sibling + sequence then rules, delay/probability + timeout fault, Analyze an authored overlap and raise priority, Simulate Run all green, live POST plus a `/ordrs/42` near-miss, workspace + WireMock export, then Start → HTTP → Assert → Stop Quick Test). Shared beat helpers in `api-mock-demo-helpers.ts` (`AM_DEMO_TIMING`, `spotlightBeat`/`spotlightElementBeat`/`clickBeat`/`fillBeat`/`selectBeat`/`revealBeat`); per-lesson E2E smoke projects (`demo-am01` … `demo-am24`) with a shared `api-mock-lesson-smoke-helpers.ts` harness; plan in `docs/plan/future/apimock/apimock-demo-curriculum-v2.md`.
- **API Mock Gallery — Literal path starting point preset** — new `am-gallery-paths` sample (a single hard-coded `GET /products/42`, no folders, no samples) that seeds the path-matching lesson and doubles as a Gallery exercise: the mock a recording gives you, waiting to be generalized into a parameterized template, a glob, or an anchored regex with the Pattern Toolbox. The demo bridge's quiet route patch (`__demoPatchApiMockActiveRoute`) now re-infers the path kind from the new path (with an explicit `pathKind` override for regex, which no path string implies), so a patched matcher is never left as an unmatchable literal.
- **API Mock Gallery — Unconditioned rule starting point preset** — new `am-gallery-predicates` sample (one `GET /reports` with an empty Match group and a paged report body) that seeds the request-conditions lesson and doubles as a Gallery exercise: the mock that answers every caller identically, waiting for query, header, cookie, security, and absence conditions to tell them apart. The demo bridge's quiet route patch (`__demoPatchApiMockActiveRoute`) now also accepts `predicates`, so a lesson can replace a whole Match tree on step replay instead of stacking duplicate rows.
- **API Mock Gallery — Body subset starting point preset** — new `am-gallery-bodies` sample (one `POST /orders` whose only condition is a forgiving `json_subset` on `{"customer":{"tier":"gold"}}`, answering `201` with a confirmation body) that seeds the body-matching lesson and doubles as a Gallery exercise: the mock that accepts any payload mentioning a gold tier, waiting to be tightened with strict equality, a JSONPath matcher, and a JSON Schema contract. Gallery presets can now ship pre-built condition trees (`StoreRouteSpec.predicates`), and the route editor exposes test ids for the JSONPath/XPath expression box, the expected-JSON textarea, and the **equals ↔ contains** match-style button so lessons and E2E can drive paired matchers.
- **API Mock Gallery — Non-JSON payloads starting point preset** — new `am-gallery-formats` sample (a urlencoded `POST /oauth/token`, a multipart `POST /uploads` answering `201`, a `POST /soap/orders` that answers with an `application/xml` envelope, and a `PUT /firmware` for raw bytes — all four with empty Match groups) that seeds the payload-formats lesson and doubles as a Gallery exercise: four endpoints that accept any body at all, waiting for form-field, multipart, XPath, XML-element-list, and digest matchers. Gallery presets can now ship XML response bodies (`StoreRouteSpec.bodyKind`).
- **API Mock Gallery — Overlapping catalog starting point preset** — new `am-gallery-selection` sample (two equal-priority `GET /catalog` rules: **Regional catalog** gated on `X-Api-Version: 2024-11`, and **Default catalog** that matches everything) that seeds the selection-policy lesson and doubles as a Gallery exercise: a real overlap waiting for nested boolean groups, a raised priority, and the two multiple-match policies. The demo bridge can now quietly patch the active server's selection policy (`__demoPatchApiMockServerSettings`).
- **API Mock Gallery — Four overlap kinds starting point preset** — new `am-gallery-overlaps` sample (eight path-disjoint rules that analyze into one Duplicate, one Shadowed, one Definite, and one Potential finding) that seeds the conflict-inspector lesson and doubles as a Gallery exercise: Analyze, filter by kind, simulate a witness, then fix or acknowledge.
- **API Mock Gallery — Static product JSON starting point preset** — new `am-gallery-templating` sample (one parameterized `GET /products/:id` answering hard-coded `{"id":"static","name":"Widget"}` with no helpers or variables) that seeds the templating lesson and doubles as a Gallery exercise: author `{{pathParam}}` / faker / server variables / Map body on top of a static contract. The response preview now evaluates templates against a sample request, and `{{jsonPath}}` supports array indexes such as `$.items[0].sku`.
- **API Mock Gallery — Plain 200 JSON starting point preset** — new `am-gallery-response` sample (one `GET /orders` answering empty `200 {}` with no extra headers or cookies) that seeds the response-content lesson and doubles as a Gallery exercise: author status, reason phrase, Format, headers, cookies, and the other body kinds on top of a blank contract. The demo bridge's quiet route patch now also accepts `status`, `reasonPhrase`, and `contentType` (which sets the body kind), so a replayed step can restore HTML or binary without forcing JSON.
- **API Mock Gallery — Payments starting point preset** — new `am-gallery-payment` sample (one `POST /payments` answering `{"ok":true,"id":"pay-1001"}` with no delay, eligibility, or faults) that seeds the timing-and-faults lesson and doubles as a Gallery exercise: author delay/jitter, match limits, expiry, probability, and connection faults on top of a plain 200. The quiet route patch now also accepts `behavior` (delay, jitter, maxMatches, expiresAt, probability, fault, chunkSchedule), and demo fetches can pass `timeoutMs` so a timeout fault is caught instead of hanging.
- **API Mock Gallery — Simulation suite preset** — new `am-gallery-suite` sample (six rules and eight saved samples with expectations: health pass, overlap ambiguous, reset fault, weighted dice, sequential-state cart, unmatched, a second health pass, and an unassociated example). Seeds the simulation-as-a-test-suite lesson. Simulate’s Assertions table can now edit expected status / body-contains, FAIL badges are stable test ids, and **Export trace** shows a filename + bundle preview.
- **Catalog — Convert / Upgrade to OpenAPI YAML** — new **Convert / Upgrade OpenAPI** action (sidebar context menu + overview button) that auto-routes on the source format: **Convert** Swagger 2.0 → OpenAPI 3.0 / 3.1, or **Upgrade** OpenAPI 3.0 → 3.1 / 3.2 and 3.1 → 3.2. The action only opens when a forward target exists (an already-latest 3.2 / unsupported spec gets a "Nothing to convert" toast). The preview modal offers a selectable engine (`swagger2openapi` default / `@scalar/openapi-upgrader` for 3.1; upgrades are Scalar-only), a structural validation gate with ✅/❌ badge + error list, validate-driven auto-fallback between engines (convert flow), normalized warnings, an on-demand **Deep lint** (`oas-validator`, advisory, 3.0.x), a copyable + searchable YAML preview, and post-conversion repair (`normalizeConvertedOpenApi3`). Output can be **downloaded** (`{name}-openapi-{target}.yaml`) or **saved as a new Catalog version** (re-parsed, changelog-tagged "Converted …" / "Upgraded …", respects the 10-version prune cap). Engines and the linter are lazy-loaded via dynamic `import()`; last-used engine/target persisted. New modules: `swaggerToOpenApi.ts`, `engines/{swagger2openapi,scalar}Engine.ts`, `convertPrefs.ts`, `openApiLint.ts`, `CatalogConvertOpenApiModal.tsx`.
- **Catalog — Prettify converted YAML (openapi-format)** — a **Prettify** toggle in the Convert / Upgrade modal (default on, persisted) sorts the converted document into a canonical, diff-friendly key order (openapi → info → servers → … → components) via `openapi-format`'s `openapiSort` before emitting YAML, applied consistently to preview, search, Copy, Download, and Save. Lazy-loaded and graceful (`prettyYaml.ts` falls back to the engine's raw YAML on any failure). Adds browser `path`/`http`/`https` shims so `openapi-format` bundles for web/desktop (it eagerly requires them for remote-`$ref` support this app never uses).
- **Catalog — Batch Convert & Compare engines** — sidebar **Batch Convert** action converts every Swagger 2.0 entry to OpenAPI 3.0 and saves each as a new version (with converted/failed/skipped summary toast); the Convert modal gains a **Compare engines** action that runs both engines at 3.0 (no fallback) and reports validity, version, warnings, and whether the two YAML outputs are identical.
- **Catalog convert demo lesson** — new 9-step Demo Hub lesson (`cat-convert-openapi`) that spotlights the **Swagger 2.0** format badge, walks engine/target selection, confirms the emitted version by searching `openapi: 3.1.1` in the preview, deep-lints the 3.0 output, demonstrates the **Prettify** toggle (off→on canonical re-sort), and highlights the new **OpenAPI 3.0.3** badge after saving; new `CAT.*` selectors + `catalogConvertAdapter` + `useDemoCatalogBridge` seed bridge.
- **Catalog docs accuracy** — import/design docs corrected to describe Swagger 2.0 as a **model normalization on import** (raw spec stored unchanged, internal `#/` `$ref` only) rather than a silent file conversion, and to reflect the custom `openApiParser.ts` (dropping outdated `@apidevtools/swagger-parser` parse/validate/convert claims).
- **gRPC Demo Hub GRPC-24 — Workflow Runner & Results** — builds a gRPC Echo workflow with `grpcTarget` variable, Quick Test, then Workflow Runner iterations, Initial Variables overrides, Results Dashboard, and Results Explorer; modular `grpc-workflow-runner-{helpers,steps}.ts` with helper unit tests (>90% coverage).
- **gRPC Load Test panel coverage split** — monolithic `GrpcLoadTestPanel.coverage-gaps.test.tsx` split into config/results modules + shared test helpers (all source files remain ≥90% product coverage).
- **gRPC Studio module splits** — `GrpcStudioPage`, `GrpcCallPanel`, `GrpcLoadTestPanel`, `GrpcSchemaDiffPanel`, and `useGrpcStudioAdvancedFeatures` decomposed into focused submodules (all under 900 lines) with co-located coverage tests ≥90%.
- **Demo Hub gRPC lesson helpers** — `grpc-lesson-helpers/` package with shared connection, streaming, TLS, mock-server, and workflow lesson steps; demo studio isolation hooks (`useDemoHubStudioIsolation`, `useDemoHubLiveDemo`).

### Added
- **Code quality refactoring — shared hooks and helpers** — extracted `useJsonTreeCollapseState` and `useMatchCountChange` hooks; added `tryParseJsonArray<T>()` to shared helpers; extracted `grpcComposerTabState.ts` from `GrpcCallPanel`; extracted `buildJTreeFromBody` and `collectJTreePaths` into `JsonTreePreview` exports; canonical JSON helper `toCanonicalJsonString` in `ResponseVersionPanel`; `WorkflowRunner` split-test setup helper `workflowRunnerSplitTestSetup.ts`. Five components wired to `useJsonTreeCollapseState`, five expression function IIFE array-parse patterns replaced, duplicate JSON-parse/import patterns consolidated across Requests, Scenarios, and GraphQL areas.

### Added
- **gRPC Phase 13A/13B SLO baselines + CI promotion** — fixture-backed baseline harness and CI gate promotion for describe/control-plane + data-plane reliability checks; reusable `grpc:phase13b:ci` orchestration script (`scripts/grpc-phase13b-ci.sh`) spins docker fixture + API server, executes strict gate, and publishes baseline artifacts for CI diagnostics.
- **gRPC Demo lesson expansion (phase 12 continuation)** — new schema discovery and streaming lesson tracks, demo storage cleanup helpers, and new sample assets under `examples/grpc/` and `public/grpc-samples/` to support end-to-end lesson walkthroughs.
- **gRPC Proto ingest contract cleanup** — `/api/grpc/describe` now accepts proto ingest input through `protoRoots` only, and route/integration regression coverage is aligned to root-based ingest across Studio, shared validation, and server paths.
- **gRPC Phase 11F — schema diff engine** — descriptor-to-descriptor comparator with Buf-style severity classification (`breaking` / `non_breaking` / `informational`); `computeGrpcSchemaDiff`, JSON/Markdown export serializers; 40 acceptance tests; `npm run test:grpc:phase11f` gate script.
- **gRPC Phase 11G — advanced feature UI** — Advanced sub-nav with Load Testing, Mock Server, and Schema Diff panels; tab-scoped operation state, progress/cancel, result export; `useGrpcStudioAdvancedFeatures` hook; `npm run test:grpc:phase11g` gate script.
- **gRPC Phase 11H — advanced export safety** — `grpcAdvancedFeatureExport` safe prepare helpers with source metadata stamping and attempt/description sanitization; leak-scan targets `grpc_load_test_export` / `grpc_schema_diff_export`; wired through advanced-features hook clipboard exports; cross-feature matrix updated; `npm run test:grpc:phase11h` gate script.
- **gRPC Phase 12B — Demo Hub lesson runtime** — in-memory runtime engine (`grpc-lesson-contract/runtime/`) with frozen scenario snapshots, step checkpoints, state machine (`idle/running/paused/completed/failed/locked`), session lifecycle; `grpcLessonRuntimeAdapter` + `useDemoHub` wiring for setup/pause/resume/restart/teardown/complete; checkpoint sync only after successful step completion (verify pass when step defines `verify`); `npm run test:grpc:phase12b` gate script.
- **gRPC Phase 12A — Demo Hub lesson contract** — frozen `grpc-lesson-contract` module (15-lesson roster, `validateGrpcDemoLesson` / roster / registry validators, `buildGrpcLessonShellFromRoster` + `buildGrpcContractMetaFromRoster`, progress migration policy); `grpcStudioAdapter` shared health probes and setup commands; GRPC-1 pilot lesson aligned to roster shell + contract meta; `npm run test:grpc:phase12a` gate script.
- **gRPC Harness Phase 8C — execution handlers** — harness `executeNonHttp` path via `grpcExecution.ts`; unary retry + server/client/bidi stream collectors; `GrpcHarnessOperations` production bridge; `RequestResult` with `transportType: 'grpcCall'` and `grpcResultMeta`; `npm run test:grpc:phase8c` gate script.
- **gRPC Harness Phase 8D — assertion engine** — harness assertion evaluator for all seven assertion kinds (`grpcStatus`, `grpcField`, `grpcNumericField`, `grpcStreamField`, `grpcTrailer`, `grpcDuration`, `grpcStreamLength`); path resolver + numeric compare; wired into `grpcExecution.ts` with `grpcResultMeta.assertionFailures`; `npm run test:grpc:phase8d` gate script.
- **gRPC Harness Phase 8E — numeric/trailer hardening** — int64/uint64-safe `grpcHarnessNumericCompare` (BigInt boundaries, mixed string/number forms, unsafe IEEE integer guard); `grpcHarnessTrailerNormalize` lowercase trailer map; wired into harness assert engine; `npm run test:grpc:phase8e` gate script.
- **gRPC Harness Phase 8F — data-source expansion** — `grpcHarnessDataSourceInterpolation` + shared `grpcHarnessAssertionTemplates` for parameterized `grpcCallAction` rows (two-phase data-source + env assertion resolution); shared `substituteBodyColumnTemplateVars` in `templateHelpers.ts`; `grpcHarnessRowIdentity` trace keys wired in rust bridge / rerun merge; `serialization` error category on snapshot-build failures; `npm run test:grpc:phase8f` gate script (35 tests).
- **gRPC Harness Phase 8G — result model** — canonical `GrpcHarnessResult` on `grpcResultMeta.harnessResult` with strict status precedence (`timeout` > `error` > `failed` > `passed`); per-assertion `assertionResults[]` with stable names; `errorCategory: 'assertion'` on harness assertion failures; `formatGrpcHarnessResultSummary` debug helper; `npm run test:grpc:phase8g` gate script.
- **gRPC Harness Phase 8H — export/redaction** — `grpcHarnessExport` redacts harness results and runner artifacts (`trailers`, bodies, diagnostics, assertion messages, `grpcStatusMessage`); workflow `grpcResultMeta` rows included; runner HTML/JSON/markdown reports redact gRPC rows; Results Dashboard Export JSON/CSV redact harness rows; `formatGrpcHarnessResultSummaryForExport`; `harness_result_export` + `runner_artifacts` leak-scan targets; `npm run test:grpc:phase8h` gate script.
- **gRPC Harness Phase 8I — hardening gate** — consolidated six-item acceptance checklist (`grpcPhase8iAcceptance.test.ts`); Phase 8 validation report and debugging runbook; `npm run test:grpc:phase8i` gate (acceptance + regression + 8A→8H chain); `npm run test:grpc:phase8` full 8A→8I sequential regression; Phase 9 entry criteria signed off.
- **gRPC Phase 9A — interpolation contracts** — frozen token grammar (`{{varName}}`), escape rules (`\{{`, `\}}`), allowed contexts, structural-field prohibitions, and error catalog; `grpcInterpolationGrammar.ts` + `grpcInterpolationContracts.ts`; `validateGrpcStructuralFieldNotTokenized`; `npm run test:grpc:phase9a` gate script.
- **gRPC Phase 9B — shared interpolation resolver** — escape-aware `resolveGrpcInterpolationTemplate` + deep JSON/metadata/auth traversal; migrated harness/workflow template resolvers, runtime context, data-source interpolation, Studio target resolution, and header endpoint preview; `npm run test:grpc:phase9b` gate script.
- **gRPC Phase 9C — precedence and env snapshot binding** — layered env-map merge (workspace → active env → profile → tab), immutable `interpolationEnv` on execute snapshots, in-flight Studio insulation on env switch, workflow VariableContext bridge; `npm run test:grpc:phase9c` gate script.
- **gRPC Phase 9D — target-specific validation** — scheme rejection, canonical `grpcHost`/`grpcPort` env validation with remediation hints, `grpcPort` derivation in `buildEnvVarMap`, shared UI/server parity; `npm run test:grpc:phase9d` gate script.
- **gRPC Phase 9E — cycle detection and diagnostic safety** — env variable dependency graph cycle detector with path trace, secret-safe interpolation diagnostics, wired into env snapshot binding and Studio target validation; `npm run test:grpc:phase9e` gate script.
- **gRPC Phase 9F — template persistence and replay compatibility** — template-only persist guards for saved requests and harness actions, tab template source at save boundary, replay re-resolution against current env; `npm run test:grpc:phase9f` gate script.
- **gRPC Phase 9G — interpolation preview and error UX** — template/resolved preview toggle on target strip, cycle/missing-token error banner with secret-safe diagnostics, deduped validation hint when banner is visible, resolved-preview redaction for secret-backed env values, header preview grammar fix for escaped literals; `npm run test:grpc:phase9g` gate script.
- **gRPC Phase 9H — cross-surface execute interpolation parity** — Studio `prepareExecuteSnapshot` and saved-request replay deep-resolve body/metadata/auth via shared `grpcInterpolationDeepResolver`; post-resolve metadata/auth validation aligned with harness/workflow builders; cross-surface matrix tests (Studio/Workflow/Harness); acceptance checklist traceability; `npm run test:grpc:phase9h` gate script (+ 9G regression).
- **gRPC Phase 9I — interpolation hardening gate** — consolidated six-item Phase 9 acceptance checklist (`grpcPhase9iAcceptance.test.ts`); mid-stream `sendStreamMessageCall` deep-interpolates body via frozen `interpolationEnv` with harness `sendMessages` parity; stream interpolation failures use `validation` category via `buildStreamValidationErrorBody` and cancel orphan server streams; Phase 9 validation report and troubleshooting runbook; `npm run test:grpc:phase9i` gate (+ 9H regression); `npm run test:grpc:phase9` full 9A→9I sequential regression; Phase 10 entry criteria signed off.
- **gRPC Phase 9 audit fixes** — harness pre-transport interpolation failures now map through `resolveGrpcInterpolationHarnessPreTransportCategory` (including `INVALID_TARGET` / missing `grpcHost`) to `serialization` instead of `internal`; harness definition version snapshots wire `prepareGrpcHarnessCallActionDefinitionSnapshot` (9F profile-only target normalization).
- **gRPC Phase 10A — transport contracts** — frozen `GrpcStudioTransportMode` union (`express`, `tauri`, `grpc-web`, `spring-servlet`), capability matrix, grpc-web content types, preflight validation (`GrpcWebTransportPreflightError` → `validation`), immutable `transportMode`/`transportSchemaVersion` on execute snapshots; wired in `prepareExecuteSnapshot`; `npm run test:grpc:phase10a` gate (+ 9I regression).
- **gRPC Phase 10B — browser transport router** — adapter registry with `dispatchReady` flags; snapshot-bound routing for unary/cancel/stream_start; express/tauri live; grpc-web/spring-servlet stubs fail closed; `npm run test:grpc:phase10b` gate (+ 10A regression).
- **gRPC Phase 10C — grpc-web framing + unary adapter** — binary/text grpc-web frame codec, canonical trailer/status normalization, browser protoset encode/decode, fetch-based unary client with AbortController cancel; grpc-web adapter `dispatchReady: true`; transport panel enables grpc-web on web builds; `npm run test:grpc:phase10c` gate (+ 10B regression).
- **gRPC Phase 10D — Spring Servlet path + unary adapter** — `/<service>/<method>` path resolver with package-qualified and short-name candidates, `application/grpc` + `TE: trailers` unary fetch client, adapter `dispatchReady: true`; `npm run test:grpc:phase10d` gate (+ 10C regression).
- **gRPC Phase 10E — browser transport error taxonomy** — stable `details.browserTransportFailure` kinds (`cors`, `proxy_unreachable`, `protocol_mismatch`, `timeout`, `server_status`), remediation hints in Response panel, Express Proxy fallback offers for grpc-web/spring-servlet; `npm run test:grpc:phase10e` gate (+ 10D regression).
- **gRPC Phase 10F — metadata/auth/tls normalization parity** — shared `buildBrowserTransportUserMetadataHeaders` for grpc-web and spring-servlet clients; `GRPC_WEB_RESERVED_HEADERS` moved to transport contracts; cross-mode metadata/auth/-bin/TLS parity acceptance tests; Phase 4 redaction regression; `npm run test:grpc:phase10f` gate (+ 10E regression).
- **gRPC Phase 10G — transport selector UX and guardrails** — `callType`-aware transport panel with per-card disabled reason labels; tab-scoped mode persistence; in-flight transport locking; execute preflight for incompatible call types; `npm run test:grpc:phase10g` gate (+ 10F regression).
- **gRPC Phase 10H — cross-surface result envelope parity** — `normalizeGrpcWebUnaryResponse` status/trailer/header contract; `GrpcCallResult` field parity across express/tauri/grpc-web/spring-servlet; harness and workflow trailer assertion parity via shared `resolveGrpcHarnessTrailerValue`; adapter `dispatchReady` and streaming boundary guards; `npm run test:grpc:phase10h` gate (+ 10G regression).
- **gRPC Phase 10I — browser transport hardening gate** — consolidated six-item Phase 10 acceptance checklist (`grpcPhase10iAcceptance.test.ts`, 89 tests); preflight/stream-boundary documentation for deferred browser-direct server streaming (runbook + transport panel hint + Express fallback on deferred `stream_start`); Spring Servlet short-path retry on HTTP 404 (incl. HTML bodies); mTLS blocked on browser-direct modes at execute preflight; frame decode failures classified as `protocol_mismatch`; Phase 10 validation report and operational runbook; `npm run test:grpc:phase10i` gate (+ 10H regression).
- **gRPC Harness Phase 8B — execution snapshots** — `GrpcHarnessExecuteSnapshot` wrapping frozen `GrpcTabExecuteSnapshot`; template resolver, snapshot builder, transport adapter, and attempt/retry lifecycle for all four call types; `prepareGrpcHarnessExecuteSnapshotExport` bridge to Phase 4H; harness validator now accepts `connectionId`-only targets and templated metadata keys/values at import (resolved at snapshot build); `npm run test:grpc:phase8b` gate script.
- **gRPC Harness Phase 8A — scenario contracts** — frozen `grpcCall` harness schema on `Scenario` (`method: 'GRPC'`, `grpcCallAction`); validators for all four call types and assertion shapes; import/run preflight gates; `npm run test:grpc:phase8a` gate script.
- **gRPC Studio Phase 3 hardening gate (3I)** — `docs/guides/grpc-phase3-runbook.md` with test matrix and descriptor troubleshooting; `npm run test:grpc:phase3` regression script (648 unit tests); E2E specs `grpc-studio-manage-schemas.spec.ts` and `grpc-studio-schema-drift.spec.ts` (blocking/rebind/dismiss/prune); describe URL SSRF smoke in `grpc-test-server.spec.ts`; `test:e2e:grpc` now 31 tests across 8 specs.
- **gRPC Studio Phase 2 streaming E2E** — Playwright specs for server, client, and bidirectional streams against the Docker echo fixture; extended `e2e/grpc-helpers.ts` streaming helpers; `npm run test:e2e:grpc` includes all live specs.
- **gRPC Studio Phase 2 runbook** — `docs/guides/grpc-phase2-runbook.md` (architecture, manual smoke, limits, acceptance checklist).
- **gRPC Studio planning document** — expanded architecture and phase plan in `docs/plan/future/grpc/grpc-studio-plan.md`.
- **`useDemoHub.coverage-resume.test.ts`** — isolated live-demo resume/session coverage tests (module-mock safe).
- **`@redfireforge/demo-hub` workspace package** (`packages/demo-hub/`): Phase 7 monorepo extraction — lessons, hub UI, adapters; separate Vitest demo project; optional public npm publish deferred.
- **Demo adapter layer** (`packages/demo-hub/`): stable bridge API for GraphQL Studio, workflow designer, environment, and app shell — lessons no longer import product hooks directly.
- **Lesson Notes panel** in Demo Hub: per-lesson scratch notes with resizable side panel and persistent storage.
- Environment Manager multi-protocol endpoint panels (GraphQL, WebSocket, SSE, Kafka).
- GraphQL Demo Hub lessons (GQL-1 through GQL-19): queries, mutations, subscriptions, batch execution, TLS, multi-tab, mock server, workflow integration, and related E2E specs.
- **§11.0 hard-refresh policy** for GraphQL demo workspace isolation — documented in `e2e/DEMO-LESSON-E2E-MEMO.md` §12; acceptance E2E 5/5 (`gql110`).
- **GQL-17/19 quality audit** — automated enhancement-tier gate in `graphql-lesson-quality-audit.test.ts` (14/14).
- **GraphQL Studio batch response UX:** per-tab response slices synced from batch runs, response banner with batch context, failed-operation pill, and **View full batch** modal entry point.
- GraphQL Studio SDL line diff viewer with golden test cases, collection import preview, profile tab-usage indicators, and schema cache in IndexedDB (v9 stores).
- GraphQL Studio batch execution UI, advanced settings, schema layer hooks, and live-demo overlay.
- Shared selector modules (`gql`, `ws`, etc.) extracted from monolithic `selectors.ts`.

### Changed
- **gRPC Demo Hub GRPC-12** — load-testing lesson updated for compact panel UI, Step 9 server-streaming highlights, and demo step pipeline timing.
- **gRPC workflow call-target fields** — `GrpcWorkflowCallTargetFields` and Assert config coverage extended; `GRPC.LESSON24_*` selectors for cross-feature demo highlights.
- gRPC local/CI readiness probes explicitly bypass host proxy interception (`curl --noproxy '*'`) to prevent false 504 failures against localhost fixture health checks.
- GraphQL demo workspace mutators (`prepareDemoWorkspace`, `closeDemoWorkspace`, etc.) no-op when `VITE_ENABLE_DEMO_HUB=false`.
- GraphQL Studio page and App shell refactored to stay under 900-line monolith limit.
- Demo Player prerequisite gate supports multi-endpoint Docker checks and GraphQL tab-budget gating.
- **Phase 8 human validation complete** — all 19 GraphQL Demo Hub lessons signed off Web + Tauri (2026-06-27).
- **Web storage:** large array blobs (catalog, workflows, environments, GraphQL Studio tabs/auth/schema) use IndexedDB only on save — no localStorage fallback for oversized payloads; empty legacy arrays are cleared without migration.
- Unit test coverage raised to >90% on every source file (statements, branches, functions, lines).

### Fixed
- **API Mock — `Form field present` never matched** — the matcher expected a `[field, value]` pair, but the operator renders a single box, so the field name arrived as a bare string and every check failed. A bare string is now read as the field name (the `exact` / `regex` variants keep their pair), matching how the multipart presence checks already behaved.
- **API Mock — closing a running server no longer reads as a deletion** — the confirm dialog for closing a tab whose listener is running now says **Stop and close** / **Stop & Close** and explains that the listener stops and the port is freed, instead of the generic "Confirm Deletion" / "Delete Permanently" / "This action cannot be undone" wording. The shared confirm dialog also exposes `data-testid` hooks (`confirm-dialog`, `confirm-title`, `confirm-accept`, `confirm-cancel`) so demos and E2E stop matching on button labels.
- gRPC Phase 13B local gate flake where localhost health/readiness checks could return proxy-generated 504 despite healthy fixture containers.
- **gRPC Studio Phase 3 hardening** — reflection always re-fetches (drift-safe); URL proto revalidates via `If-None-Match`; BSR compares digest on reload; block `https://` loopback in SSRF policy; preserve `lastKnownGoodDescriptor` when reflect resolves invalid target; fix Manage Schemas modal Escape during load; add URL/BSR `describeFromIngest` hook tests and loopback SSRF E2E.
- **gRPC streaming SSE late-attach race:** fast server streams finalized before the browser connected SSE — registry now buffers events and replays on attach; terminal entries retained until grace elapses.
- **gRPC stream compose panel:** log virtualizer could overlap Send/End controls — flex clipping and z-index fix in `grpc-studio.css`.
- **gRPC Clear log + SSE dedupe:** `clearStreamLog` no longer resets `lastSequence` — prevents replaying cleared rows after tab switch or SSE reconnect.
- **gRPC stream send/end errors:** failed `sendStreamMessage` / `endStream` now best-effort `DELETE` cancel so server registry does not orphan active streams.
- **gRPC stream registry cleanup:** sync transport start failures and terminal SSE replay now finalize registry entries immediately (no 60s orphan).
- **gRPC method detail panel:** streaming methods show “Streaming-ready” hint instead of stale “execution arrives in Phase 2” copy.
- **`useDemoHub` auto-play pause:** `toggleAutoPlay` now pauses via `isPlayingRef` so `pauseAutoPlay()` runs reliably when stopping playback.
- PrerequisiteGate infinite re-render loop (`probeEndpoints` memoization).
- GraphQL endpoint badge hostname display (localhost vs 127.0.0.1).
- Whitespace-only per-tab GraphQL endpoints now inherit profile/page default (batch endpoint parity).
- GQL-15 batch demo lesson: history panel hidden on step 1, step 3 tab-add flow, step 9 highlights batch modal on partial errors.
- Monaco/vitest test environment polyfills, IDB/storage test mocks, and assorted GraphQL route/TLS handler edge cases.

---

## Recent Release Highlights

### 0.6.x
- Major workflow, testing, and platform reliability improvements.
- Kafka and protocol feature depth expanded.
- Coverage, type safety, and E2E stability improvements.

### 0.5.x
- Data Mapper, validation, and results explorer refinements.
- Workflow UX and storage reliability improvements.

> Historical details and exhaustive release notes were moved to `CHANGELOG.archive.md` to reduce documentation token overhead.
