/**
 * Roster alias. The lesson lives in `api-mock-am13.ts` (namespace imports) so a
 * poisoned HMR graph on that URL can be replaced with a valid module.
 */
export { apiMockAm13Lesson } from './api-mock-am13';
